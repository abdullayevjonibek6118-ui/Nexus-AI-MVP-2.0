# 🔒 SECURITY.md - Безопасность Nexus AI

## Политика Безопасности

Мы серьёзно относимся к безопасности. Этот документ описывает реализованные меры безопасности и рекомендации.

---

## Реализованные Меры Безопасности

### 1. Аутентификация и Авторизация

#### JWT Токены
- �� Токены истекают через 60 минут
- ✅ Секретный ключ хранится в `.env`
- ✅ Использование алгоритма HS256

#### Пароли
- ✅ Хеширование с bcrypt (cost factor 12)
- ✅ Минимальная длина: 8 символов
- ⚠️ Рекомендуется: требовать цифры, спецсимволы

### 2. Защита API

#### CORS (Cross-Origin Resource Sharing)
```python
#  Разрешенные origins
BACKEND_CORS_ORIGINS = [
    "http://localhost:3000",
    "http://127.0.0.1:3000"
]
```

#### Rate Limiting
⚠️ **TODO**: Добавить в production
```python
# Рекомендуется: slowapi или FastAPI-Limiter
@limiter.limit("100/minute")
async def endpoint():
    ...
```

#### Input Validation
- ✅ Pydantic схемы для всех входных данных
- ✅ Автоматическая валидация типов
- ✅ Email валидация

### 3. Защита от Инъекций

#### SQL Injection
- ✅ SQLAlchemy ORM (параметризованные запросы)
- ✅ Отсутствие сырого SQL
- ✅ Prepared statements

#### XSS (Cross-Site Scripting)
```javascript
// В чате: эскейп HTML
function escapeHtml(text) {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
}
```

⚠️ **TODO**: Добавить санитизацию на frontend

#### Path Traversal
- ✅ Валидация имен файлов
- ✅ Ограничение типов файлов (PDF, DOCX, TXT)

### 4. Безопасность Данных

#### Чувствительные Данные
```env
# ❌ НЕ КОММИТИТЬ .env файл в Git!
# Добавьте в .gitignore:
.env
*.env.local
```

#### Логирование
- ✅ Не логируются пароли
- ✅ Не логируются API ключи
- ⚠️ TODO: Маскировка email в логах production

#### Database
- ✅ SQLite для MVP
- ⚠️ Рекомендуется PostgreSQL для production
- ✅ Регулярные бэкапы (автоматизировать)

---

## Рекомендации для Production

### 1. HTTPS
```nginx
# nginx конфигурация
server {
    listen 443 ssl http2;
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
}
```

### 2. Environment Variables
```bash
# Генерация секретного ключа
python -c "import secrets; print(secrets.token_urlsafe(32))"

# Использование в .env
SECRET_KEY="сгенерированный-ключ-длиной-минимум-32-символа"
```

### 3. Database Security
```python
# PostgreSQL вместо SQLite
DATABASE_URL="postgresql://user:password@localhost:5432/nexus_ai"

# Шифрование соединения
DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"
```

### 4. API Key Protection
```python
# Ротация API ключей
OPENROUTER_API_KEY="обновляйте-регулярно"

# Мониторинг использования
# Установите лимиты на openrouter.ai
```

### 5. Content Security Policy
```python
# В FastAPI middleware
@app.middleware("http")
async def add_security_headers(request, call_next):
    response = await call_next(request)
    response.headers["Content-Security-Policy"] = "default-src 'self'"
    response.headers["X-Content-Type-Options"] = "nosniff"
    response.headers["X-Frame-Options"] = "DENY"
    return response
```

---

## Уязвимости и Отчеты

### Как Сообщить об Уязвимости

Если вы обнаружили уязвимость безопасности:

1. **НЕ создавайте публичный Issue**
2. Отправьте email на: **security@nexus-ai.com**
3. Включите:
   - Описание уязвимости
   - Шаги воспроизведения
   - Потенциальное влияние
   - Предложенное исправление (опционально)

### Процесс Обработки

1. **Подтверждение**: В течение 48 часов
2. **Оценка**: 3-5 рабочих дней
3. **Исправление**: В зависимости от серьёзности
4. **Уведомление**: После выпуска патча

###  CVE и Обновления

- Критические: Патч в течение 24 часов
- Высокий приоритет: 1 неделя
- Средний: 2 недели
- Низкий: Следующий релиз

---

## Checklist для Развертывания

### Production Readiness

- [ ] HTTPS настроен (SSL/TLS)
- [ ] Секретные ключи сгенерированы и безопасно хранятся
- [ ] Database на PostgreSQL с шифрованием
- [ ] Rate limiting включен
- [ ] CORS ограничен конкретными доменами
- [ ] CSP headers добавлены
- [ ] Логирование настроено (без чувствительных данных)
- [ ] Monitoring включен (Sentry/New Relic)
- [ ] Регулярные бэкапы автоматизированы
- [ ] Firewall настроен
- [ ] Dependency scanning (Dependabot/Snyk)
- [ ] Security tests регулярно запускаются

### Code Review

- [ ] Проверка всех входных данных
- [ ] Нет хардкоженных секретов
- [ ] Все SQL запросы параметризованы
- [ ] XSS защита в местах вывода пользовательского контента
- [ ] CSRF токены для форм (если нужно)
- [ ] Правильная обработка ошибок (без утечки информации)

---

## Инструменты для Аудита

### Автоматизированное Сканирование

```bash
# Safety - проверка известных уязвимостей в зависимостях
pip install safety
safety check

# Bandit - статический анализ Python кода
pip install bandit
bandit -r backend/app/

# OWASP Dependency-Check
dependency-check --project "Nexus AI" --scan backend/

# npm audit (для frontend, если используется)
npm audit
```

### Ручное Тестирование

- Burp Suite для тестирования API
- OWASP ZAP для поиска уязвимостей
- Postman для тестирования аутентификации

---

## Compliance и Стандарты

### GDPR (если применимо)
- Право на удаление данных
- Экспорт персональных данных
- Согласие на обработку

### Локальные Законы РФ
- 152-ФЗ "О персональных данных"
- Хранение данных на территории РФ (если требуется)

---

## Контакты

- **Security Team**: security@nexus-ai.com
- **Bug Bounty**: Нет (пока)
- **Emergency**: Telegram @nexus_ai_security

---

**Последнее обновление**: 08.01.2026  
**Версия**: 2.0
